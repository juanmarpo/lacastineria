<p id="notice"><%= notice %></p>

<h1>Listing Directors</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Nombre</th>
      <th>Contacto</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @directors.each do |director| %>
      <tr>
        <td><%= director.id %></td>
        <td><%= director.name %></td>
        <td><%= director.contact %></td>
        <td><%= director.email %></td>
        <td><%= link_to 'Ver', director %></td>
        <td><%= link_to 'Editar datos', edit_director_path(director) %></td>
        <td><%= link_to 'Borrar', director, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Director', new_director_path %>
